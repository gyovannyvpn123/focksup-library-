/**
 * Fișier punct de intrare pentru biblioteca Focksup
 * Permite importul simplu: require('focksup-library')
 */

// Exportăm toate componentele din biblioteca compilată (dist)
module.exports = require('./dist/src/index.js');